<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cabral.com.appmanaos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_sinetram"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_sinetram"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TelaLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TelaPrincipal"/>
        <activity android:name=".Slade_Cpf"/>
        <activity android:name=".NovoCadastro"/>

    </application>

</manifest>